# E11-S5 / NFR-8: route for the existing Caddy proxy (latam_ai_workbench-proxy).
# Applying it touches a NEIGHBOURING container, so it is delivered as a snippet for
# ops approval (NFR-5) instead of being applied by the glue.
#
# Inside the proxy's Caddyfile:
#   import llmwiki.conf
#
# llwiki.<your-internal-domain> {
# 	reverse_proxy 127.0.0.1:20080
# 	header {
# 		Strict-Transport-Security "max-age=31536000"
# 		X-Content-Type-Options nosniff
# 	}
# }
