apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nginx-demo.fullname" . }}-secret
  labels:
    {{- include "nginx-demo.labels" . | nindent 4 }}
type: Opaque
data:
  {{- toYaml .Values.secret.data | nindent 2 }}
